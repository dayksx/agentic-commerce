<<<<<<< Updated upstream
=======
version: "3.8"

>>>>>>> Stashed changes
services:
  agentic-commerce:
    build:
      context: .
      dockerfile: Dockerfile
<<<<<<< Updated upstream
    image: docker.io/cesarev/agentic-commerce
    platform: linux/amd64
    container_name: agentic-commerce
    ports:
      - "8001:8001"
      - "3000:3000"
    volumes:
      - /run/rofl-appd.sock:/run/rofl-appd.sock
    environment:
      PRIVATE_KEY: ${PRIVATE_KEY}
      CDP_API_KEY_ID: ${CDP_API_KEY_ID}
      CDP_API_KEY_SECRET: ${CDP_API_KEY_SECRET}
      CDP_WALLET_SECRET: ${CDP_WALLET_SECRET}
=======
    container_name: agentic-commerce
    ports:
      - "8001:8001"
    environment:
      - NODE_ENV=production
      - MCP_REQUIRE_PAYMENT=${MCP_REQUIRE_PAYMENT:-true}
      - MCP_ENDPOINT=${MCP_ENDPOINT:-http://0.0.0.0:8001/mcp}
      - PRIVATE_KEY=${PRIVATE_KEY}
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:8001/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
>>>>>>> Stashed changes
    networks:
      - agentic-network

networks:
  agentic-network:
    driver: bridge
